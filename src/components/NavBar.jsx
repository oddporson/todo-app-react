import "./navbar.css";

const NavBar = () => {
  return (
    <nav className="container">
      <div className="nav">
        <h2>Tasks Lists</h2>
        <button>Add List</button>
      </div>
    </nav>
  );
};

export default NavBar;
